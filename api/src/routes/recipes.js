const { Router } = require('express');
const axios = require('axios');

const { Recipe, Type, recipe_type } = require('../db.js'); //importo los modelos conectados


const router = Router();

module.exports = router;